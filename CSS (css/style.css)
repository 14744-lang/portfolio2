*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:"Prompt",sans-serif;
}

body{
    background:linear-gradient(135deg,#ffffff,#eaf6ff,#d7eeff);
    color:#2d3e50;
}

header{
    background:rgba(255,255,255,.8);
    backdrop-filter:blur(10px);
    box-shadow:0 3px 15px rgba(0,0,0,.08);
    position:sticky;
    top:0;
    z-index:999;
}

nav{
    display:flex;
    justify-content:center;
    gap:30px;
    padding:18px;
}

nav a{
    text-decoration:none;
    color:#4da6ff;
    font-weight:bold;
    transition:.3s;
}

nav a:hover{
    color:white;
    background:#69b8ff;
    padding:8px 18px;
    border-radius:30px;
}

.hero{
    text-align:center;
    padding:80px 20px;
}

.hero img{
    width:220px;
    height:220px;
    border-radius:50%;
    border:8px solid white;
    box-shadow:0 10px 30px rgba(0,0,0,.15);
}

.hero h1{
    margin-top:25px;
    font-size:45px;
    color:#55aaff;
}

.hero p{
    margin-top:15px;
    font-size:20px;
}

.container{
    width:90%;
    max-width:1100px;
    margin:auto;
    padding:50px 0;
}

.card{
    background:white;
    border-radius:25px;
    padding:35px;
    margin-bottom:30px;
    box-shadow:0 10px 30px rgba(0,0,0,.08);
}

.card h2{
    color:#55aaff;
    margin-bottom:20px;
}

.gallery{
    display:grid;
    grid-template-columns:repeat(auto-fit,minmax(280px,1fr));
    gap:20px;
}

.gallery img{
    width:100%;
    border-radius:20px;
    transition:.4s;
}

.gallery img:hover{
    transform:scale(1.05);
}

.btn{
    display:inline-block;
    margin-top:20px;
    background:#6bbcff;
    color:white;
    padding:14px 30px;
    border-radius:30px;
    text-decoration:none;
    transition:.3s;
}

.btn:hover{
    background:#459eff;
}

footer{
    background:#69b8ff;
    color:white;
    text-align:center;
    padding:20px;
    margin-top:50px;
}
